# Afterglow Robot PRD

## 1. 产品概述

### 1.1 产品名称
Afterglow Robot

### 1.2 产品目标
构建一个**本地优先、文本优先、授权纪念用途**的人物表达风格复刻系统。  
用户提供目标微信号与已解密的微信聊天记录目录后，系统自动完成文本数据处理、RAG 配置、本地模型接入与微信接入，让用户在微信中与“该人物风格”的机器人进行文本对话。

### 1.3 目标用户

- 个人本地纪念用途用户
- 在合法授权前提下整理个人或家庭聊天数据的用户
- 需要一个可继续扩展为语音能力的开发团队

## 2. 产品范围

### 2.1 一期范围

一期必须支持：

1. 用户输入目标微信号或 `self`
2. 用户输入已解密导出的聊天记录目录
3. 系统自动发现联系人并过滤目标文本消息
4. 系统构建 few-shot、RAG 语料与本地向量索引
5. 系统生成人设提示词
6. 系统配置本地 `Ollama`
7. 系统安装或校验 `OpenClaw` 与微信插件
8. 用户扫码后，在微信内完成文本聊天

### 2.2 非一期范围

一期不包含：

- 语音生成与发送
- 表情自动发送
- 多角色组合人格
- 模型微调
- 群聊场景
- 云端 SaaS 化
- 可视化管理后台

## 3. 问题定义

用户当前的问题是：

- 微信聊天数据虽然能导出，但不能直接用于人物风格复刻
- 本地 LLM、RAG、微信接入需要多个工具协同，手工配置门槛高
- 目标是“尽量像某个人说话”，而不是通用知识型问答

因此产品需要解决：

1. 数据筛选标准化
2. 风格语料构建标准化
3. 本地模型和 RAG 配置标准化
4. 微信接入步骤标准化

## 4. 核心用户流程

### 4.1 初始化流程

1. 用户准备微信导出目录
2. 用户输入：
   - `target_wxid` 或 `self`
   - `wechat_export_dir`
3. 系统执行环境检查
4. 系统安装或校验依赖
5. 系统解析导出目录并发现联系人
6. 系统提取目标文本消息
7. 系统清洗消息并构建 few-shot 与 RAG 语料
8. 系统写入 `Chroma`
9. 系统生成 `persona_prompt.txt`
10. 系统输出微信接入准备说明

### 4.2 在线聊天流程

1. 微信用户发送消息
2. OpenClaw 将消息转发给本地服务
3. 系统检索相似历史表达
4. 系统拼装 prompt
5. 系统调用 `Ollama`
6. 系统返回文本回复

## 5. 输入输出定义

### 5.1 必填输入

- `target_wxid`
  - 字符串
  - 支持普通微信号标识
  - 支持特殊值 `self`
- `wechat_export_dir`
  - 本地目录路径
  - 指向用户通过 `WeChatDataAnalysis` 导出的结果目录

### 5.2 系统输出

- `contacts.json`
- `messages.normalized.jsonl`
- `fewshot.json`
- `rag_corpus.jsonl`
- `persona_prompt.txt`
- `models/chroma_db/`

### 5.3 在线输出

- 微信中的文本回复
- 本地日志
- 可选调试输出文件

## 6. 功能需求

### FR-1 环境检查
系统必须检查：

- `Python`
- `Node.js`
- `npx`
- `Ollama`
- OpenClaw 相关依赖

若依赖缺失，系统需给出明确提示或执行安装脚本。

### FR-2 数据目录解析
系统必须能读取用户提供的导出目录，并识别其中可用账号与数据结构。

### FR-3 联系人发现
系统必须能列出联系人，并支持：

- 自动识别单账号场景
- 多账号场景下要求显式指定账号

### FR-4 目标消息筛选
系统必须支持：

- 按 `target_wxid` 提取该对象发出的全部文本消息
- 按 `self` 提取号主本人发出的全部文本消息

### FR-5 文本清洗
系统必须过滤：

- 系统消息
- 非文本消息
- 空文本
- 低信息量短文本

### FR-6 数据集构建
系统必须生成：

- few-shot 样本
- RAG 检索语料
- persona prompt

### FR-7 向量索引
系统必须将 RAG 语料写入本地 `Chroma`。

### FR-8 本地推理
系统必须通过 `Ollama` 进行文本生成，并可用 few-shot、历史窗口和检索结果组装 prompt。

### FR-9 微信接入
系统必须支持通过以下命令安装微信接入能力：

`npx -y @tencent-weixin/openclaw-weixin-cli@latest install`

系统需提供扫码前准备说明与配置校验能力。

## 7. 非功能需求

### NFR-1 隐私

- 默认不上传云端
- 默认本地持久化
- 日志不直接打印完整敏感文本

### NFR-2 可维护性

- 模块职责单一
- 配置集中管理
- 安装逻辑与业务逻辑分离

### NFR-3 可扩展性

需为后续能力预留扩展点：

- TTS
- 语音消息发送
- 表情发送
- 多模型切换

### NFR-4 平台范围

- 一期默认面向 `Windows`
- 默认本地单用户使用

## 8. 成功标准

满足以下条件即视为一期达标：

1. 用户可输入目标微信号与聊天记录目录
2. 系统可生成结构化文本产物与 `Chroma` 索引
3. 系统可生成 `persona_prompt.txt`
4. 系统可调用本地 `Ollama` 生成文本回复
5. 用户扫码后可在微信中与机器人进行文本聊天

## 9. 验收标准

### 文档验收

- README 包含 `wx_key` 与 `WeChatDataAnalysis` 的手工步骤
- README 清楚区分“已规划”和“已实现”
- `PRD.md` 明确一期边界与隐私约束
- `TECH_DESIGN.md` 明确接口、数据流和测试方案

### 功能验收

- 单账号目录可自动识别
- 多账号目录需显式指定账号
- `target_wxid` 与 `self` 均可正确筛选文本消息
- few-shot、RAG、向量索引可稳定生成
- 缺少模型或配置时返回明确错误
- 微信侧能完成文本聊天闭环

## 10. 风险与约束

### 风险

- 微信导出目录结构可能因版本变化而变化
- OpenClaw 或微信插件能力可能调整
- 本地模型效果受硬件与模型本身影响

### 约束

- 不实现微信数据破解逻辑
- 不实现云端服务
- 不承诺“完全像本人”

## 11. 版本路线

### Phase 0
完成 `README`、`PRD`、`TECH_DESIGN`

### Phase 1
完成离线文本导入与数据构建

### Phase 2
完成本地文本回复闭环

### Phase 3
完成 `init` 与 `wechat-connect`

### Phase 4
完成微信文本聊天接入

### Phase 5
仅新增语音扩展接口定义
